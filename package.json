{
  "name": "korochin-pageshot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test", "check": "node --check background.js && node --check capture.js && node --check popup.js" }
}
